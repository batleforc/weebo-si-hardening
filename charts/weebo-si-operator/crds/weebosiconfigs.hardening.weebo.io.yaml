apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: weebosiconfigs.hardening.weebo.io
spec:
  group: hardening.weebo.io
  names:
    kind: WeeboSiConfig
    plural: weebosiconfigs
    singular: weebosiconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for WeeboSiConfigSpec via `CustomResource`
        properties:
          spec:
            description: '`spec` of the `WeeboSiConfig` CRD.'
            properties:
              features:
                default: {}
                description: One optional field per registered feature.
                properties:
                  dwocPin:
                    description: '`spec.features.dwocPin`.'
                    nullable: true
                    properties:
                      catalog:
                        description: Every DWOC a workspace is permitted to run with.
                        items:
                          description: |-
                            One entry of `spec.features.dwocPin.catalog`. Serializes flat, as `{key, name, namespace}`
                            per the RFC's own examples — not `{key, target: {name, namespace}}`.
                          properties:
                            key:
                              description: The short identifier a grant, an annotation, or a resolved decision names.
                              type: string
                            name:
                              description: The `DevWorkspaceOperatorConfig`'s name.
                              type: string
                            namespace:
                              description: The namespace it lives in.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        type: array
                      default:
                        description: The entry for a namespace belonging to no team.
                        type: string
                      grants:
                        additionalProperties:
                          description: |-
                            What one team may reach: a non-empty set of catalogue keys, and the one among them a
                            namespace with no more specific answer gets.
                          properties:
                            allowed:
                              description: The catalogue keys this team may reach.
                              items:
                                description: |-
                                  A short identifier for a catalogue entry, unique within the catalogue. Never a
                                  `{name, namespace}` pair — see RFC 0002's *Why keys rather than references*.
                                type: string
                              type: array
                            default:
                              description: The one among `allowed` a namespace with no more specific answer gets.
                              type: string
                          required:
                          - allowed
                          - default
                          type: object
                        default: {}
                        description: What each team may reach, keyed by team name.
                        type: object
                      mode:
                        description: 'Required, per the chassis: `Off` | `DryRun` | `Enforce`.'
                        enum:
                        - Off
                        - DryRun
                        - Enforce
                        type: string
                      namespaceSelection:
                        default:
                          annotation: hardening.weebo.io/dwoc
                          onUnknownKey: Default
                        description: The namespace annotation naming a catalogue key.
                        properties:
                          annotation:
                            default: hardening.weebo.io/dwoc
                            description: |-
                              Namespace annotation naming a catalogue key. The empty string disables namespace
                              selection entirely.
                            type: string
                          onUnknownKey:
                            default: Default
                            description: What to do when that annotation names a key the namespace cannot reach.
                            enum:
                            - Default
                            - Deny
                            type: string
                        type: object
                      namespaceSelector:
                        description: 'Optional, per the chassis: narrows within the webhook''s own scope.'
                        nullable: true
                        properties:
                          matchExpressions:
                            default: []
                            description: Every expression must hold.
                            items:
                              description: One `matchExpressions` entry.
                              properties:
                                key:
                                  description: The label key this expression tests.
                                  type: string
                                operator:
                                  description: How `values` is compared against the label's value, if any.
                                  enum:
                                  - In
                                  - NotIn
                                  - Exists
                                  - DoesNotExist
                                  type: string
                                values:
                                  default: []
                                  description: The comparison values `operator` reads. Unused by `Exists`/`DoesNotExist`.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                          matchLabels:
                            additionalProperties:
                              type: string
                            default: {}
                            description: Every key must be present with exactly this value.
                            type: object
                        type: object
                      onMissingTarget:
                        default: Skip
                        description: What to do when the resolved entry does not point at a live DWOC.
                        enum:
                        - Skip
                        - Deny
                        type: string
                    required:
                    - catalog
                    - default
                    - mode
                    type: object
                  imagePolicy:
                    description: '`spec.features.imagePolicy`, per RFC 0005.'
                    nullable: true
                    properties:
                      catalog:
                        description: Every entry a workspace may be granted. Required, non-empty.
                        items:
                          description: |-
                            One entry of `spec.features.imagePolicy.catalog`: a key, and the image patterns it permits.

                            **Carries no scope, no exception and no negation**, per RFC 0005's *A pattern set is a
                            union*: selecting more entries can only ever permit more, so an `except` field would be a
                            rule whose meaning depends on which other entries happen to be selected.
                          properties:
                            key:
                              description: The short identifier a grant, an attribute, or the top-level `default` names.
                              type: string
                            patterns:
                              description: |-
                                The image patterns this entry permits. Non-empty; parsed by `weebo-si-image-policy`, and
                                held here as the text an admin wrote so the CRD stays readable by `kubectl` and GitOps.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - patterns
                          type: object
                        type: array
                      default:
                        description: |-
                          Applied to a namespace belonging to no team, and to a team with no grant. Required; may
                          be empty, which means the platform set and nothing else.
                        items:
                          description: |-
                            A short identifier for a catalogue entry, unique within the catalogue. Same rationale as
                            `CatalogKey` in `dwoc_pin` and `ProfileKey` in `network_profiles`: a key is never a
                            `{name, namespace}` pair, and never a bare `String` a team name could be passed as.
                          type: string
                        type: array
                      grants:
                        additionalProperties:
                          description: |-
                            What one team may reach: the set of entry keys it may reach, and the subset applied when a
                            workspace asks for nothing. Either may legitimately be empty — an ungranted team reaches
                            only the platform set.
                          properties:
                            allowed:
                              default: []
                              description: The entry keys this team may reach.
                              items:
                                description: |-
                                  A short identifier for a catalogue entry, unique within the catalogue. Same rationale as
                                  `CatalogKey` in `dwoc_pin` and `ProfileKey` in `network_profiles`: a key is never a
                                  `{name, namespace}` pair, and never a bare `String` a team name could be passed as.
                                type: string
                              type: array
                            default:
                              default: []
                              description: The subset of `allowed` applied when a workspace names nothing more specific.
                              items:
                                description: |-
                                  A short identifier for a catalogue entry, unique within the catalogue. Same rationale as
                                  `CatalogKey` in `dwoc_pin` and `ProfileKey` in `network_profiles`: a key is never a
                                  `{name, namespace}` pair, and never a bare `String` a team name could be passed as.
                                type: string
                              type: array
                          type: object
                        default: {}
                        description: What each team may reach, keyed by team name.
                        type: object
                      mode:
                        description: 'Required, per the chassis: `Off` | `DryRun` | `Enforce`.'
                        enum:
                        - Off
                        - DryRun
                        - Enforce
                        type: string
                      namespaceSelection:
                        default:
                          annotation: hardening.weebo.io/image-policy
                        description: The namespace annotation naming an entry key list.
                        properties:
                          annotation:
                            default: hardening.weebo.io/image-policy
                            description: |-
                              Namespace annotation naming a comma-separated entry key list. The empty string disables
                              this selection step entirely.
                            type: string
                        type: object
                      namespaceSelector:
                        description: 'Optional, per the chassis: narrows within the webhooks'' own scope.'
                        nullable: true
                        properties:
                          matchExpressions:
                            default: []
                            description: Every expression must hold.
                            items:
                              description: One `matchExpressions` entry.
                              properties:
                                key:
                                  description: The label key this expression tests.
                                  type: string
                                operator:
                                  description: How `values` is compared against the label's value, if any.
                                  enum:
                                  - In
                                  - NotIn
                                  - Exists
                                  - DoesNotExist
                                  type: string
                                values:
                                  default: []
                                  description: The comparison values `operator` reads. Unused by `Exists`/`DoesNotExist`.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                          matchLabels:
                            additionalProperties:
                              type: string
                            default: {}
                            description: Every key must be present with exactly this value.
                            type: object
                        type: object
                      onNotGranted:
                        default: Default
                        description: |-
                          What to do when a workspace names an entry key its team's grant does not allow.

                          Reuses `dwoc-pin`'s [`OnUnknownKey`] rather than `network-profiles`' `OnNotGranted`: the
                          two enums are the same `Default`/`Deny` pair, and a third copy of it would be a third
                          place to keep in step. The field name is this RFC's own (`onNotGranted`), because that
                          is the vocabulary its *Contract* table uses.
                        enum:
                        - Default
                        - Deny
                        type: string
                      platform:
                        default:
                          builtin: true
                          extra: []
                        description: The always-allowed platform pattern set.
                        properties:
                          builtin:
                            default: true
                            description: |-
                              Whether the compiled-in platform patterns apply. The compiled-in list is explicitly
                              **not** contract (RFC 0005's *Stability*) — it tracks Che and DevWorkspace Operator.
                            type: boolean
                          extra:
                            default: []
                            description: |-
                              Additional always-allowed patterns, for an admin who mirrors the platform images into
                              their own registry.
                            items:
                              type: string
                            type: array
                        type: object
                      variables:
                        additionalProperties:
                          description: |-
                            One declared pattern variable's binding. Exactly one binding form ships —
                            `fromNamespaceAnnotation` — per RFC 0005's *Future work*: "so the surface stays one thing
                            rather than a small language."
                          properties:
                            fromNamespaceAnnotation:
                              description: The namespace annotation key this variable's value is read from.
                              type: string
                          required:
                          - fromNamespaceAnnotation
                          type: object
                        default: {}
                        description: |-
                          Additional pattern variables beyond the two built in, name → binding. Declaring one is
                          the opt-in to an annotation-sourced value, and to the RBAC dependency RFC 0005's
                          *Security considerations* spends a section on.
                        type: object
                      workspaceSelection:
                        default:
                          attribute: hardening.weebo.io/image-policy
                        description: The devfile attribute naming an entry key list.
                        properties:
                          attribute:
                            default: hardening.weebo.io/image-policy
                            description: |-
                              DevWorkspace attribute naming a comma-separated entry key list. The empty string disables
                              this selection step entirely.
                            type: string
                        type: object
                    required:
                    - catalog
                    - default
                    - mode
                    type: object
                  kubearmorPolicy:
                    description: '`spec.features.kubearmorPolicy`, per RFC 0006.'
                    nullable: true
                    properties:
                      baseline:
                        description: The profile applied to every workspace pod in scope, never negotiable.
                        type: string
                      catalog:
                        description: Every runtime profile a workspace may be granted.
                        items:
                          description: |-
                            One entry of `spec.features.kubearmorPolicy.catalog`.

                            Carries one [`TemplateRef`] directly rather than a `variants` list, unlike
                            [`crate::network_profiles::Profile`]: there is exactly one backend today, and the
                            variant-per-backend shape is deferred until a second one actually exists rather than
                            speculatively built now — RFC 0006's *Alternatives considered*.
                          properties:
                            key:
                              description: The short identifier a grant, an attribute, or a resolved decision names.
                              type: string
                            templateRef:
                              description: The `KubeArmorPolicy` object this entry copies its rule content from.
                              properties:
                                name:
                                  description: The template object's name.
                                  type: string
                                namespace:
                                  description: The namespace it lives in.
                                  type: string
                              required:
                              - name
                              - namespace
                              type: object
                          required:
                          - key
                          - templateRef
                          type: object
                        type: array
                      enforcement:
                        default:
                          backend: Auto
                          defaultPosture:
                            capabilities: Audit
                            file: Audit
                            network: Audit
                        description: The enforcement engine and the default posture written onto namespaces in scope.
                        properties:
                          backend:
                            default: Auto
                            description: Which engine to write policy for.
                            enum:
                            - Auto
                            - KubeArmor
                            type: string
                          defaultPosture:
                            default:
                              capabilities: Audit
                              file: Audit
                              network: Audit
                            description: What happens in each rule domain when nothing matches.
                            properties:
                              capabilities:
                                default: Audit
                                description: Unmatched capability use.
                                enum:
                                - Audit
                                - Block
                                type: string
                              file:
                                default: Audit
                                description: Unmatched file *and process* operations.
                                enum:
                                - Audit
                                - Block
                                type: string
                              network:
                                default: Audit
                                description: Unmatched network operations.
                                enum:
                                - Audit
                                - Block
                                type: string
                            type: object
                        type: object
                      grants:
                        additionalProperties:
                          description: |-
                            What one team may reach: the set of runtime profile keys it may reach, and the subset of them
                            applied when a workspace asks for nothing. Either may legitimately be empty — an ungranted
                            team reaches only the baseline.
                          properties:
                            allowed:
                              default: []
                              description: The runtime profile keys this team may reach.
                              items:
                                description: |-
                                  A short identifier for a runtime profile catalogue entry, unique within the catalogue. Never
                                  a `{name, namespace}` pair — same rationale as `CatalogKey` in `dwoc_pin` and `ProfileKey` in
                                  `network_profiles`.
                                type: string
                              type: array
                            default:
                              default: []
                              description: The subset of `allowed` applied when a workspace names nothing more specific.
                              items:
                                description: |-
                                  A short identifier for a runtime profile catalogue entry, unique within the catalogue. Never
                                  a `{name, namespace}` pair — same rationale as `CatalogKey` in `dwoc_pin` and `ProfileKey` in
                                  `network_profiles`.
                                type: string
                              type: array
                          type: object
                        default: {}
                        description: What each team may reach, keyed by team name.
                        type: object
                      mode:
                        description: 'Required, per the chassis: `Off` | `DryRun` | `Enforce`.'
                        enum:
                        - Off
                        - DryRun
                        - Enforce
                        type: string
                      namespaceSelection:
                        default:
                          annotation: hardening.weebo.io/kubearmor-policy
                        description: The namespace annotation naming a runtime profile key list.
                        properties:
                          annotation:
                            default: hardening.weebo.io/kubearmor-policy
                            description: |-
                              Namespace annotation naming a comma-separated key list. The empty string disables this
                              selection step entirely.
                            type: string
                        type: object
                      namespaceSelector:
                        description: 'Optional, per the chassis: narrows within the controller''s own scope.'
                        nullable: true
                        properties:
                          matchExpressions:
                            default: []
                            description: Every expression must hold.
                            items:
                              description: One `matchExpressions` entry.
                              properties:
                                key:
                                  description: The label key this expression tests.
                                  type: string
                                operator:
                                  description: How `values` is compared against the label's value, if any.
                                  enum:
                                  - In
                                  - NotIn
                                  - Exists
                                  - DoesNotExist
                                  type: string
                                values:
                                  default: []
                                  description: The comparison values `operator` reads. Unused by `Exists`/`DoesNotExist`.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                          matchLabels:
                            additionalProperties:
                              type: string
                            default: {}
                            description: Every key must be present with exactly this value.
                            type: object
                        type: object
                      onNotGranted:
                        default: Default
                        description: What to do when a workspace names a key its team's grant does not allow.
                        enum:
                        - Default
                        - Deny
                        type: string
                      workspaceSelection:
                        default:
                          attribute: hardening.weebo.io/kubearmor-policy
                        description: The devfile attribute naming a runtime profile key list.
                        properties:
                          attribute:
                            default: hardening.weebo.io/kubearmor-policy
                            description: |-
                              DevWorkspace attribute naming a comma-separated key list. The empty string disables this
                              selection step entirely.
                            type: string
                        type: object
                    required:
                    - baseline
                    - catalog
                    - mode
                    type: object
                  networkProfiles:
                    description: '`spec.features.networkProfiles`, per RFC 0004.'
                    nullable: true
                    properties:
                      baseline:
                        description: The profile applied to every namespace in scope, never negotiable.
                        type: string
                      catalog:
                        description: Every profile a workspace may be granted.
                        items:
                          description: One entry of `spec.features.networkProfiles.catalog`.
                          properties:
                            key:
                              description: The short identifier a grant, an attribute, or a resolved decision names.
                              type: string
                            variants:
                              description: This profile's variants, at most one per [`Backend`].
                              items:
                                description: One backend's rendering of a profile. A profile carries at most one variant per backend.
                                properties:
                                  backend:
                                    description: Which dialect this variant is written in.
                                    enum:
                                    - NetworkPolicy
                                    - Cilium
                                    type: string
                                  templateRef:
                                    description: The template object this variant copies `policyTypes`/`ingress`/`egress` from.
                                    properties:
                                      name:
                                        description: The template object's name.
                                        type: string
                                      namespace:
                                        description: The namespace it lives in.
                                        type: string
                                    required:
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - backend
                                - templateRef
                                type: object
                              type: array
                          required:
                          - key
                          - variants
                          type: object
                        type: array
                      enforcement:
                        default:
                          backend: Auto
                          canary:
                            enabled: true
                            intervalSeconds: 300
                        description: The enforcement backend and its canary.
                        properties:
                          backend:
                            default: Auto
                            description: Which policy dialect to write.
                            enum:
                            - Auto
                            - NetworkPolicy
                            - Cilium
                            type: string
                          canary:
                            default:
                              enabled: true
                              intervalSeconds: 300
                            description: The periodic enforcement probe.
                            properties:
                              enabled:
                                description: Whether the canary probe runs.
                                type: boolean
                              intervalSeconds:
                                description: How often the probe runs.
                                format: uint32
                                minimum: 0.0
                                type: integer
                            required:
                            - enabled
                            - intervalSeconds
                            type: object
                        type: object
                      grants:
                        additionalProperties:
                          description: |-
                            What one team may reach: the set of profile keys it may reach, and the subset of them applied
                            when a workspace asks for nothing. Either may legitimately be empty — an ungranted team
                            reaches only the baseline.
                          properties:
                            allowed:
                              default: []
                              description: The profile keys this team may reach.
                              items:
                                description: |-
                                  A short identifier for a catalogue entry, unique within the catalogue. Never a
                                  `{name, namespace}` pair — same rationale as `CatalogKey` in `dwoc_pin`.
                                type: string
                              type: array
                            default:
                              default: []
                              description: The subset of `allowed` applied when a workspace names nothing more specific.
                              items:
                                description: |-
                                  A short identifier for a catalogue entry, unique within the catalogue. Never a
                                  `{name, namespace}` pair — same rationale as `CatalogKey` in `dwoc_pin`.
                                type: string
                              type: array
                          type: object
                        default: {}
                        description: What each team may reach, keyed by team name.
                        type: object
                      mode:
                        description: 'Required, per the chassis: `Off` | `DryRun` | `Enforce`.'
                        enum:
                        - Off
                        - DryRun
                        - Enforce
                        type: string
                      namespaceSelection:
                        default:
                          annotation: hardening.weebo.io/network-profiles
                        description: The namespace annotation naming a profile key list.
                        properties:
                          annotation:
                            default: hardening.weebo.io/network-profiles
                            description: |-
                              Namespace annotation naming a comma-separated profile key list. The empty string disables
                              this selection step entirely.
                            type: string
                        type: object
                      namespaceSelector:
                        description: 'Optional, per the chassis: narrows within the controller''s own scope.'
                        nullable: true
                        properties:
                          matchExpressions:
                            default: []
                            description: Every expression must hold.
                            items:
                              description: One `matchExpressions` entry.
                              properties:
                                key:
                                  description: The label key this expression tests.
                                  type: string
                                operator:
                                  description: How `values` is compared against the label's value, if any.
                                  enum:
                                  - In
                                  - NotIn
                                  - Exists
                                  - DoesNotExist
                                  type: string
                                values:
                                  default: []
                                  description: The comparison values `operator` reads. Unused by `Exists`/`DoesNotExist`.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                          matchLabels:
                            additionalProperties:
                              type: string
                            default: {}
                            description: Every key must be present with exactly this value.
                            type: object
                        type: object
                      onNotGranted:
                        default: Default
                        description: What to do when a workspace names a profile key its team's grant does not allow.
                        enum:
                        - Default
                        - Deny
                        type: string
                      workspaceSelection:
                        default:
                          attribute: hardening.weebo.io/network-profiles
                        description: The devfile attribute naming a profile key list.
                        properties:
                          attribute:
                            default: hardening.weebo.io/network-profiles
                            description: |-
                              DevWorkspace attribute naming a comma-separated profile key list. The empty string
                              disables this selection step entirely.
                            type: string
                        type: object
                    required:
                    - baseline
                    - catalog
                    - mode
                    type: object
                  policyGuard:
                    description: '`spec.features.policyGuard`, per RFC 0004.'
                    nullable: true
                    properties:
                      allowedIdentities:
                        default: []
                        description: Identities exempt from the guard's rules, in addition to the operator's own.
                        items:
                          type: string
                        type: array
                      mode:
                        description: 'Required, per the chassis: `Off` | `DryRun` | `Enforce`.'
                        enum:
                        - Off
                        - DryRun
                        - Enforce
                        type: string
                      namespaceSelector:
                        description: 'Optional, per the chassis: narrows within the webhook''s own scope.'
                        nullable: true
                        properties:
                          matchExpressions:
                            default: []
                            description: Every expression must hold.
                            items:
                              description: One `matchExpressions` entry.
                              properties:
                                key:
                                  description: The label key this expression tests.
                                  type: string
                                operator:
                                  description: How `values` is compared against the label's value, if any.
                                  enum:
                                  - In
                                  - NotIn
                                  - Exists
                                  - DoesNotExist
                                  type: string
                                values:
                                  default: []
                                  description: The comparison values `operator` reads. Unused by `Exists`/`DoesNotExist`.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                          matchLabels:
                            additionalProperties:
                              type: string
                            default: {}
                            description: Every key must be present with exactly this value.
                            type: object
                        type: object
                    required:
                    - mode
                    type: object
                type: object
              teams:
                default: []
                description: Chassis-level, ordered, first match wins.
                items:
                  description: |-
                    One entry of `spec.teams`. `spec.teams` as a whole is ordered and first-match-wins — that
                    rule is applied by [`crate::dwoc_pin::DwocPinConfig::validate`] and by the resolution chain
                    in `weebo-si-dwoc-pin`, not encoded on this type.
                  properties:
                    name:
                      description: This team's identity, referenced by every feature's `grants`.
                      type: string
                    namespaceSelector:
                      description: The namespaces that belong to this team.
                      properties:
                        matchExpressions:
                          default: []
                          description: Every expression must hold.
                          items:
                            description: One `matchExpressions` entry.
                            properties:
                              key:
                                description: The label key this expression tests.
                                type: string
                              operator:
                                description: How `values` is compared against the label's value, if any.
                                enum:
                                - In
                                - NotIn
                                - Exists
                                - DoesNotExist
                                type: string
                              values:
                                default: []
                                description: The comparison values `operator` reads. Unused by `Exists`/`DoesNotExist`.
                                items:
                                  type: string
                                type: array
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                        matchLabels:
                          additionalProperties:
                            type: string
                          default: {}
                          description: Every key must be present with exactly this value.
                          type: object
                      type: object
                  required:
                  - name
                  - namespaceSelector
                  type: object
                type: array
            type: object
          status:
            description: |-
              `status` of the `WeeboSiConfig` CRD. Entirely derived from `spec` and the feature registry —
              deleting it costs one reconcile, per RFC 0002's *Data and state*.
            nullable: true
            properties:
              conditions:
                default: []
                description: 'Standard `metav1.Condition` list: `Ready`, `Degraded`.'
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              features:
                default: []
                description: One entry per registered feature.
                items:
                  description: |-
                    One entry of `status.features`.

                    `camelCase` on the wire like every other type in this schema — it was missing the attribute
                    until 2026-08-25, which serialised `observedGeneration` as `observed_generation` and made
                    this one field disagree with both its parent and RFC 0002's own examples.
                  properties:
                    message:
                      description: 'Human-readable detail — e.g. "evaluated 214 workspaces: 6 would be replaced."'
                      type: string
                    name:
                      description: The feature's kebab-case identifier.
                      type: string
                    observedGeneration:
                      description: The `spec.metadata.generation` this status was computed from.
                      format: int64
                      type: integer
                    state:
                      description: The feature's reported state.
                      enum:
                      - Disabled
                      - DryRun
                      - Active
                      - Degraded
                      type: string
                  required:
                  - message
                  - name
                  - observedGeneration
                  - state
                  type: object
                type: array
              observedGeneration:
                default: 0
                description: The `spec.metadata.generation` this status reflects.
                format: int64
                type: integer
            type: object
        required:
        - spec
        title: WeeboSiConfig
        type: object
    served: true
    storage: true
    subresources:
      status: {}
